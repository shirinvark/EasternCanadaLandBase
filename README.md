After knitting and rendering `EasternCanadaLandbase.Rmd`, overwrite this file with a symlink to `EasternCanadaLandbase.md`.

E.g., on Linux/macOS from terminal:

```bash
cd E:/EasternCanadaLandBase/EasternCanadaLandbase
rm README.md && ln -s EasternCanadaLandbase.md README.md
```
